# Canonical site origin. Leave unset locally to use http://localhost:3000.
# On Vercel, set this to your production domain (e.g. https://example.com).
# An empty value will be ignored; Vercel preview/production URLs are used instead.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
